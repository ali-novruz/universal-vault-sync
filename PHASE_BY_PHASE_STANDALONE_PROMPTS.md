# 🚀 UNIVERSAL AI VAULT SYNC — STANDALONE PHASE-BY-PHASE EXECUTION PROMPTS

> **System Designation:** Universal AI Vault Sync Modular Execution Suite  
> **How to Use:** Run each phase prompt sequentially in your agent or IDE. Each prompt is 100% self-contained, contains all necessary architectural context, and specifies exact code deliverables.

---

# 📦 PROMPT 1: PHASE 1 — Spatial Grid, Design Tokens & Ergonomic Foundation

```markdown
# 🏛️ UNIVERSAL AI VAULT SYNC // EXECUTION PROMPT: PHASE 1
## Domain: Spatial Rhythm, OKLCH/HSL Design Tokens, Typography Scales & Ergonomics

### 1. CONTEXT & OBJECTIVE
You are engineering the foundational Design Token System and Core CSS Architecture for **Universal AI Vault Sync**—the cross-agent persistent memory protocol connecting Claude Code, Google Antigravity, Cursor, Windsurf, Aider, Cline, and Copilot to a centralized Obsidian PARA Vault.
In this Phase 1, you must establish an uncompromising, high-precision typographic, spatial, and color token system in `index.css`.

### 2. ORCHESTRATED DISCIPLINES
- `frontend-design`, `high-end-visual-design`, `css-native`, `desktop-principles`, `mobile-principles`, `modern-web-guidance-plugin`

### 3. TECHNICAL SPECIFICATIONS & RULES

1. **8-Point Spatial Rhythm & CSS Variables:**
   - Define exact spacing tokens: `--space-2xs: 4px; --space-xs: 8px; --space-sm: 12px; --space-md: 16px; --space-lg: 24px; --space-xl: 32px; --space-2xl: 48px; --space-3xl: 64px; --space-4xl: 96px;`.
   - Implement fluid container widths: `--container-max: 1440px; --container-narrow: 1100px;`.

2. **Perceptual OKLCH / HSL Color Architecture:**
   - **Deep Substrates:** `--bg-base: #08090d; --bg-surface: #0f121a; --bg-card: #151a26; --bg-glass: rgba(15, 18, 26, 0.75);`
   - **Luminous Synaptic Cyan:** `--cyan-sync: #00f0ff; --cyan-glow: rgba(0, 240, 255, 0.35); --cyan-dim: #008fa0;`
   - **Knowledge Vault Amethyst:** `--purple-vault: #a855f7; --purple-glow: rgba(168, 85, 247, 0.35);`
   - **Deterministic Emerald:** `--green-verified: #10b981; --green-glow: rgba(16, 185, 129, 0.35);`
   - **Agent Hue Matrix:**
     - `--agent-claude: #d97706; --agent-antigravity: #3b82f6; --agent-cursor: #f43f5e; --agent-windsurf: #14b8a6; --agent-aider: #eab308; --agent-cline: #8b5cf6; --agent-copilot: #64748b;`
   - **Lines & Borders:** `--line-subtle: rgba(255, 255, 255, 0.08); --line-focus: rgba(0, 240, 255, 0.4);`

3. **Fluid Typography Scales (`clamp()`):**
   - Headings: `font-family: 'Syne', sans-serif;` with letter-spacing tracking (`-0.03em`).
     - Display Hero: `font-size: clamp(2.8rem, 6.5vw, 5.2rem); line-height: 0.95;`
     - Section Title: `font-size: clamp(1.8rem, 3.5vw, 2.6rem); line-height: 1.1;`
   - Technical Monospace: `font-family: 'JetBrains Mono', 'Space Mono', monospace; font-variant-numeric: tabular-nums;`
     - Telemetry & Headers: `font-size: clamp(0.72rem, 1.2vw, 0.85rem); letter-spacing: 0.15em; text-transform: uppercase;`
   - Body & Prose: `font-family: 'Inter', 'Plus Jakarta Sans', sans-serif; font-size: clamp(0.95rem, 1.1vw, 1.1rem); line-height: 1.65;`

4. **Ergonomic Layouts & Touch Targets:**
   - Full dynamic viewport support (`min-height: 100dvh;`).
   - Mobile touch targets conforming to Hoober thumb-zone guidelines (minimum `44x44px` clickable hitboxes).
   - Multi-layer glassmorphism: `backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px);`.

### 4. DELIVERABLES
Create or update `index.css` with the full token system, resets, typography rules, glassmorphism utilities, and responsive CSS grid/flex primitives.
```

---

# 📦 PROMPT 2: PHASE 2 — Anti-Generic Direction & Visual Dial Calibration

```markdown
# 🏛️ UNIVERSAL AI VAULT SYNC // EXECUTION PROMPT: PHASE 2
## Domain: Editorial Layout Architecture, Semantic HTML5, Multi-Module HUD & Visual Tension

### 1. CONTEXT & OBJECTIVE
Transform `index.html` into an ultra-modern, editorial-grade web application layout for **Universal AI Vault Sync**. Reject generic centered card templates and establish an asymmetrical, high-density HUD layout that reflects real cross-agent synchronizations.

### 2. ORCHESTRATED DISCIPLINES
- `design-taste-frontend`, `design-dna`, `paint`, `desktop-principles`, `mobile-principles`

### 3. THE THREE DESIGN DIALS CALIBRATION
- **Density (75% Compact Precision):** High-density telemetry readouts, compact parameter bars, and split-screen code comparisons with zero wasted space.
- **Boldness (85% Architectural Authority):** High-contrast monochromatic typography paired with vibrant luminous vector channels.
- **Elegance (90% Swiss Craft):** Laser-etched border lines (`1px solid rgba(255,255,255,0.08)`), multi-layered glass panels, and live status badges.

### 4. TECHNICAL SPECIFICATIONS & VIEWPORT MODULES

Construct `index.html` with the following 5 semantic landmark modules:

1. **Header & Status Bar (`<header id="site-header">`):**
   - Brand logo: `UNIVERSAL // VAULT SYNC` with pulsing green deterministic status indicator (`100% COVERAGE`).
   - Quick navigation links: `#module-hero`, `#module-condenser`, `#module-matrix`, `#module-para`, `#module-radar`.
   - Audio telemetry toggle button (`id="audio-toggle"`).

2. **Hero Viewport & 3D HUD (`<section id="module-hero">`):**
   - Background WebGL Canvas: `<canvas id="webgl-canvas"></canvas>`.
   - Asymmetrical editorial hero copy with primary CTA (`INITIALIZE MEMORY SYNC`) and secondary documentation link.
   - Floating real-time Agent Status Pill Matrix (Claude, Antigravity, Cursor, Windsurf, Aider, Cline, Copilot).

3. **Module 1: Live Stream Log Condenser (`<section id="module-condenser">`):**
   - Split-pane layout:
     - *Left Pane:* Streaming noisy JSONL raw transcript terminal (`id="raw-jsonl-stream"`).
     - *Center Conduit:* Visual AST condensation pipeline metrics (`78.4% Token Reduction`, `12ms Latency`, SHA-256 Checksum).
     - *Right Pane:* Rendered Markdown preview of `Chats/YYYY-MM-DD.md` with executive summary tags.

4. **Module 2: Cross-Agent Context Porting Matrix (`<section id="module-matrix">`):**
   - Interactive 3-step scenario player with tab triggers:
     - *Step 1: Claude CLI* -> Architectural decision recorded.
     - *Step 2: Obsidian Vault Core* -> Background condensation in `<12ms`.
     - *Step 3: Cursor / Antigravity IDE* -> Instant context hydration without re-prompting.

5. **Module 3: PARA Filesystem & 100% Test Radar (`<section id="module-para">` & `<section id="module-radar">`):**
   - Glassmorphic PARA file tree navigator (`01_Projects`, `02_Areas`, `03_Resources`, `04_Archive`).
   - Node.js test runner telemetry dashboard with 4 SVG circular metric gauges (Statements: 100%, Branches: 100%, Functions: 100%, Lines: 100%).

### 5. DELIVERABLES
Generate a fully semantic, valid HTML5 `index.html` file incorporating all unique descriptive IDs, ARIA landmarks, and modular markup structures.
```

---

# 📦 PROMPT 3: PHASE 3 — 60 FPS Kinetic Choreography & Motion Architecture

```markdown
# 🏛️ UNIVERSAL AI VAULT SYNC // EXECUTION PROMPT: PHASE 3
## Domain: GSAP 3.12 Timelines, ScrollTrigger Scrubbing, Lenis Inertia & GPU Acceleration

### 1. CONTEXT & OBJECTIVE
Implement the kinetic choreography and motion architecture for **Universal AI Vault Sync**. Ensure smooth 60 FPS scroll-linked reveals, seamless state transitions, and zero Cumulative Layout Shift (CLS).

### 2. ORCHESTRATED DISCIPLINES
- `awwwards-animations`, `gsap-core`, `gsap-timeline`, `gsap-scrolltrigger`, `motion-principles`

### 3. TECHNICAL SPECIFICATIONS & RULES

1. **Strict GPU Acceleration Discipline:**
   - Animate strictly via hardware-accelerated CSS properties: `transform: translate3d(x, y, 0)`, `scale()`, `rotate()`, and `opacity`.
   - Apply `will-change: transform, opacity` hints during active animation cycles and clean them up upon completion.
   - Absolutely zero animation of layout-triggering properties (`width`, `height`, `top`, `margin`, `padding`).

2. **GSAP & ScrollTrigger Setup:**
   - Initialize GSAP and register `ScrollTrigger`.
   - Configure scrubbed hero camera rotation timeline linked to scroll progress (`ScrollTrigger.create({ scrub: 1 })`).
   - Implement staggered reveal animations for telemetry cards and code panels (`gsap.from('.telemetry-card', { stagger: 0.08, y: 30, opacity: 0, duration: 0.8, ease: 'power3.out' })`).

3. **Interactive UI Micro-Interactions:**
   - Magnetic hover effects on CTA buttons (`#trigger-sync`) using client bounding rectangle deltas.
   - Smooth tab switching in the Cross-Agent Matrix with cross-fade opacity transitions.
   - Animated progress bars and rotating status rings on log condensing events.

4. **Lifecycle & Memory Management:**
   - Provide clean teardown mechanisms on window resize or page reloads (`ScrollTrigger.refresh()`).
   - Prevent layout thrashing by caching bounding box calculations outside animation loops.

### 4. DELIVERABLES
Implement the kinetic choreography module in `app.js` or `motion.js`, wiring GSAP timelines and interactive hover physics to all UI elements.
```

---

# 📦 PROMPT 4: PHASE 4 — Immersive WebGL, PBR Materials & Custom GLSL Shaders

```markdown
# 🏛️ UNIVERSAL AI VAULT SYNC // EXECUTION PROMPT: PHASE 4
## Domain: Three.js 3D Force Graph, PBR Physical Shaders, Dynamic Bezier Splines & Web Audio

### 1. CONTEXT & OBJECTIVE
Construct the immersive WebGL/Three.js centerpiece and Web Audio telemetry suite for **Universal AI Vault Sync**. The 3D scene represents the decentralized cross-agent neural memory graph connected to the central Obsidian Vault.

### 2. ORCHESTRATED DISCIPLINES
- `threejs-fundamentals`, `threejs-geometry`, `threejs-materials`, `threejs-lighting`, `threejs-shaders`, `canvas-generative`

### 3. TECHNICAL SPECIFICATIONS & SCENE TOPOLOGY

1. **Scene, Camera & Responsive Viewport:**
   - High-performance `THREE.WebGLRenderer` attached to `#webgl-canvas` with `antialias: true, alpha: true`.
   - Cap Device Pixel Ratio: `renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))`.
   - Responsive `THREE.PerspectiveCamera(45, width / height, 0.1, 1000)` with smooth mouse orbit damping.

2. **Nodes Hierarchy & PBR Materials:**
   - **Central Core Node (The Obsidian Vault):**
     - Geometry: `THREE.IcosahedronGeometry(2.4, 2)` or multi-faceted crystalline lathe.
     - Material: `THREE.MeshPhysicalMaterial` with deep obsidian color (`#090c14`), transmission `0.85`, roughness `0.12`, metalness `0.3`, and IOR `1.52`.
     - Wireframe Lattice: Outer glowing amethyst wireframe cage (`THREE.WireframeGeometry`).
   - **7 Orbiting Satellite Agent Nodes:**
     - Distinct colored nodes positioned in 3D space for Claude (`#d97706`), Antigravity (`#3b82f6`), Cursor (`#f43f5e`), Windsurf (`#14b8a6`), Aider (`#eab308`), Cline (`#8b5cf6`), and Copilot (`#64748b`).
     - Luminous glow halos using additive blending sprite materials.

3. **Dynamic Bezier Splines & Particle Flow:**
   - Construct quadratic Bezier curves (`THREE.QuadraticBezierCurve3`) connecting each satellite agent node to the central Obsidian core.
   - Animate luminous data packet particles along the splines with configurable velocities upon synchronization triggers.

4. **Custom GLSL Shader Wave Displacements:**
   - Implement custom vertex shader displacing wireframe lattice vertices with sinusoidal time harmonics:
     `vec3 newPos = position + normal * (sin(position.x * 2.0 + uTime * 1.5) * 0.15);`
   - Implement Fresnel edge glow in fragment shader:
     `float fresnel = pow(1.0 - dot(viewDir, normal), 3.0);`

5. **Web Audio API Acoustic Telemetry Suite:**
   - Procedural Web Audio API oscillator synthesis (zero MP3/WAV dependencies):
     - **Vault Sync Chime:** Dual-sine harmonic chime at `880Hz` & `1760Hz` with exponential gain decay.
     - **Stream Data Pulse:** Lowpass-filtered `110Hz` rhythmic blip on transcript compression.
     - **Node Click:** Crisp tactile `2400Hz` micro-click.
   - Global mute toggle with persistence in `localStorage`.

### 4. DELIVERABLES
Implement the complete Three.js WebGL engine, shader uniforms, particle animation loops, raycaster interaction, and Web Audio API synthesizer in `app.js`.
```

---

# 📦 PROMPT 5: PHASE 5 — Exhaustive Heuristic Audit, TDD & Verification Gate

```markdown
# 🏛️ UNIVERSAL AI VAULT SYNC // EXECUTION PROMPT: PHASE 5
## Domain: Heuristic Audit, WCAG AAA Compliance, Cross-Browser Testing & Port 5500 Live Audit

### 1. CONTEXT & OBJECTIVE
Execute an exhaustive heuristic audit, automated test coverage inspection, and live browser verification on **Universal AI Vault Sync** at `http://127.0.0.1:5500/index.html`. Verify zero console errors, 60 FPS rendering performance, and complete functional fidelity.

### 2. ORCHESTRATED DISCIPLINES
- `ui-ux-pro-max`, `design-audit`, `systematic-debugging`, `chrome-devtools-plugin`, `verification-before-completion`

### 3. AUDIT CHECKLIST & VERIFICATION PROTOCOLS

1. **Accessibility & WCAG AAA Verification:**
   - [ ] Verify color contrast ratio >= 4.5:1 for all prose text and >= 3:1 for all UI badges against `#08090d`.
   - [ ] Confirm semantic HTML5 landmark tags (`<header>`, `<main>`, `<section>`, `<nav>`, `<aside>`, `<footer>`).
   - [ ] Verify descriptive `aria-label` attributes on all buttons, canvas elements, and interactive tab switches.
   - [ ] Ensure full keyboard navigation support (Tab indexing and focus rings).

2. **Responsive Fidelity & Device Emulation:**
   - [ ] Desktop Workstation (1920x1080): Complete split-pane layouts and multi-agent HUD.
   - [ ] Tablet (768px): Responsive grid reflow and touch-friendly controls.
   - [ ] Mobile Viewport (375px): Single-column stack, minimum 44x44px touch targets, and zero horizontal overflow.

3. **Functional Simulation & Sandbox Verification:**
   - [ ] Clicking `INITIALIZE SYNC` triggers particle pulse animation in Three.js, plays the 880Hz audio chime, and updates the condenser log stream.
   - [ ] Switching Cross-Agent Matrix tabs smoothly swaps context panes between Claude CLI, Obsidian Vault, and Cursor IDE.
   - [ ] PARA file tree explorer dynamically expands/collapses folders and displays markdown previews.
   - [ ] 4 SVG circular metric gauges correctly indicate 100.0% coverage across Statements, Branches, Functions, and Lines.

4. **Console & Performance Telemetry:**
   - [ ] 0 unhandled runtime exceptions in browser console.
   - [ ] 0 WebGL context loss warnings.
   - [ ] Sustained 60 FPS animation loop confirmed via requestAnimationFrame profiling.

### 4. DELIVERABLES
Audit the live deployment on `http://127.0.0.1:5500/index.html`, apply minimal surgical patches to fix any identified defects, and produce the final verification report.
```
