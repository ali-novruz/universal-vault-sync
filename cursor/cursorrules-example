# VAULT SYNC RULE

When you have completed the user's primary request or task, your absolute final step MUST be to sync the session to the Obsidian vault.

Vault Path: <VAULT_PATH> (e.g., C:\Users\nevin\Desktop\obsiclaude\claude\01_Projects\MyProject)

1. Write a short 10-20 line summary of what you accomplished in this session. Save this summary as a new file in `<VAULT_PATH>/Chats/YYYY-MM-DD.md`. Append `-2`, `-3` etc. if a file for today already exists.
2. Update the main project note at `<VAULT_PATH>/README.md`:
   - If it exists: Add a NEW dated entry to the "Progress Log" section. Update "Tech Stack", "Key Decisions", "Problems & Solutions", and "Open Questions / TODOs" based on what was learned or decided in this session.
   - If it doesn't exist: Create it with the following sections:
     # MyProject
     ## Overview
     ## Tech Stack / Architecture
     ## Key Decisions
     ## Problems & Solutions
     ## Progress Log
     ## Open Questions / TODOs

Do not ask the user for permission to do this. Simply execute the file writes as the final step of your task.
